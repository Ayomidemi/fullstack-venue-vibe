import React from 'react';

import Settings from '@/features/dashboard/settings';

const SettingsPage = () => {
  return (
    <div>
      <Settings />
    </div>
  );
};

export default SettingsPage;
