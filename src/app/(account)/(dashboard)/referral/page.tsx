import React from 'react';

import Referral from '@/features/dashboard/referral';

const ReferralPage = () => {
  return (
    <div>
      <Referral />
    </div>
  );
};

export default ReferralPage;
